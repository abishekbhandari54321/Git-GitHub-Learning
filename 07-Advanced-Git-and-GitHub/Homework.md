# Homework

## Task 1

Create:

practice-diff.txt

Modify it and use:

```bash
git diff
```

---

## Task 2

Restore the file using:

```bash
git restore practice-diff.txt
```

---

## Task 3

Create:

old-file.txt

Commit it and rename it using:

```bash
git mv old-file.txt new-file.txt
```

---

## Task 4

Create:

delete-file.txt

Delete it using:

```bash
git rm delete-file.txt
```

Commit and push the deletion.

---

## Task 5

Run:

```bash
git show
git show <commit-id>
```

Observe the output.

---

## Task 6

Run:

```bash
git config --list
git config --global user.name
git config --global user.email
git config --local --list
git config --system --list
```

Observe the differences between the configurations.
